<header>
    <nav class="navBackground">

        <div class="logoContent">
            <a class="cat" href="/">HowTo?</a>
        </div>

        <div class="searchBarContent m-3">
            <form class="searchBar">
                <input onkeyup="onKeyUp(this)" name="search" class="bg-transparent border-0 w-100" placeholder="Search"/>

              <!--  <button class="border-0 bg-transparent seachBarButton">
                    <i class="fas fa-search"></i>
                </button> -->

            </form>
        </div>

        <div class="menu">
            <a class="cat" href="{{ path("tutorial_list")}}">Tutoriels</a>
            {% if app.user %}
                <a class="cat" href="{{ path("user_account")}}">Mon compte</a>
            {% else %}
                <a class="cat" href="{{ path("app_login")}}">Se connecter</a>
            {% endif %}
            
        </div>

    </nav>
</header>